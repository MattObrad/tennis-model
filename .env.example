# Tennis prediction pipeline — environment variables
# Copy to .env and fill in real values.
# .env is gitignored and must never be committed.

# VPS Postgres password (host/user/db are in config.json)
VPS_DB_PASSWORD=your_postgres_password

# Discord webhook for tennis edge alerts (green, 3066993)
DISCORD_WEBHOOK_TENNIS=https://discord.com/api/webhooks/YOUR/WEBHOOK

# VPS deploy credentials
VPS_HOST=198.199.77.143
VPS_USER=picks
VPS_KEY_PATH=C:/Users/matto/.ssh/id_ed25519
